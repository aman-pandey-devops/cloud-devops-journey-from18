      
                                                                              DAY 24
                                                                           Python Loops
1> What is loop?
= A loop is a programming concept used to repeat the same block
  of code multiple times.

 # It is useful when we need to perform the same task again
   without writing the same code repeatedly.

2> for Loop
= A for loop is used when we already know how many times we
 wanr to repeat a task.

  # Syntax
 for i in range(5):
     print("Hello")

 
 #1> range(stop)
 
 = range(5) strat from 0 and stop before 5.

 #2> range(start, stop)
 
 = range(1, 6)
  Starts from 1 and stops before 6.

 #3> range(start, stop, step)

 = range(2, 11, 2)
 start = 2
 stop = 11 ( 11 is not included)
 step = 29jump by 2 each time)



3> The i Variable

= i is just a temporary variable.

 it stores one value from range() during each iteration.

 Example:
 for i in range(4):
       print("cloud" , i)

  Output:
cloud 0
cloud 1
cloud 2
cloud 3

4> while Loop
=  A while loop runs as long as the condition is true.


5> break Statement
= The break statement immediately stops the loop.

Example:
   for i in range(1,11):
         if i == 5:
      print(i)

6> continue Statement
= The continue statement skips the current iteration and moves
  to tge next one.

Example:
  for i in range(1,6):
       if i == 3:
            continue
      print(i)

 
